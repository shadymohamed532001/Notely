import 'package:flutter/material.dart';

class VerfideView extends StatelessWidget {
  const VerfideView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(children: []),
    );
  }
}
